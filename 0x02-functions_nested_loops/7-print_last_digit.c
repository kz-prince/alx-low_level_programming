#include "main.h"

/**
 * print_last_digit - prints the last digit
 * @n: the number
 * Return: Value of the last digit
 */
int print_last_digit(int n)
{
	int ld = n % 10;

	if (ld < 0)
		ls *= -1;

	_putcher(ld + '0");
	return (0);
}

